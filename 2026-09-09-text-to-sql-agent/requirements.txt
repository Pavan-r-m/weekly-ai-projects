# Core script uses only the Python standard library (sqlite3, re, dataclasses).
# This dependency is optional -- only needed if you set ANTHROPIC_API_KEY to
# use the real LLM backend instead of the built-in rule-based fallback.
anthropic>=0.34.0
